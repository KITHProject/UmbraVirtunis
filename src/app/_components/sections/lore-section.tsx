import { CardHover } from "../3d-card-hover"

export default function LoreSection() {
  return (
    <section className="mt-12 flex flex-col items-center md:flex-row">
      <CardHover />
    </section>
  )
}
